import 'package:flutter/material.dart';

class LomtScreen extends StatelessWidget {
  const LomtScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('LomtScreen'),
      ),
    );
  }
}
