import 'package:flutter/material.dart';

class HeightpredresultScreen extends StatelessWidget {
  const HeightpredresultScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('HeightpredresultScreen'),
      ),
    );
  }
}
